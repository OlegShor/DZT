sudo pvcreate /dev/vdb
sudo vgcreate volume1 /dev/vdb /dev/sdc
sudo lvcreate -l 100%FREE -n vol-test volum1
sudo mkfs.xfs /dev/volume1/storage
sudo mount /dev/volume1/storage /storage
sudo echo '/dev/volume1/storage /storage XFS rw 0 0' >> /etc/fstab
